# Application
# In production (e.g. Vercel), set to your app URL: https://your-app.vercel.app
NEXT_PUBLIC_URL=http://localhost:3000
NODE_ENV=development

# Database
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jobsfinder

# Authentication
# Generate AUTH_SECRET with: openssl rand -base64 32
AUTH_SECRET=your-auth-secret-minimum-32-characters-long
AUTH_GITHUB_ID=your-github-oauth-client-id
AUTH_GITHUB_SECRET=your-github-oauth-client-secret
AUTH_GOOGLE_ID=your-google-oauth-client-id
AUTH_GOOGLE_SECRET=your-google-oauth-client-secret

# File Upload
UPLOADTHING_TOKEN=your-uploadthing-token
UPLOADTHING_SECRET=your-uploadthing-secret

# Security
ARCJET_KEY=your-arcjet-api-key

# Payments
SECRET_STRIPE_KEY=sk_test_your-stripe-secret-key
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret

# Email
RESEND_API_KEY=re_your-resend-api-key
RESEND_FROM_EMAIL=JobsFinder <onboarding@resend.dev>

# Admin
# Comma-separated list of admin email addresses
ADMIN_EMAILS=admin@example.com

# Inngest (Optional - for local development)
# Events are sent to http://localhost:8288 in dev by default. Override if your dev server runs elsewhere:
# INNGEST_EVENT_API_BASE_URL=http://localhost:8288
INNGEST_EVENT_KEY=your-inngest-event-key
INNGEST_SIGNING_KEY=your-inngest-signing-key
# Set to "true" to test job expiration in dev (jobs expire after ~2 min)
# ENABLE_JOB_EXPIRATION_IN_DEV=true
